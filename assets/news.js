(()=>{const controls=document.querySelector('.news-filters');if(!controls)return;controls.hidden=false;const cards=[...document.querySelectorAll('.news-card')];function filter(value){let n=0;for(const card of cards){card.hidden=value!=='all'&&card.dataset.category!==value;if(!card.hidden)n++;}document.querySelector('#news-count').textContent=n+' headlines';document.querySelector('#news-empty').hidden=n!==0;for(const b of controls.querySelectorAll('button'))b.setAttribute('aria-pressed',String(b.dataset.filter===value));}controls.addEventListener('click',e=>{const b=e.target.closest('button[data-filter]');if(b)filter(b.dataset.filter);});filter('all');})();